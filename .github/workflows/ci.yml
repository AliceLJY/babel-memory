name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    name: bun test + build + types
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: oven-sh/setup-bun@v2
      - run: bun install
      - run: bun test
      - run: bun run build
      - name: typecheck
        run: bunx tsc --noEmit
      - name: dist must not inline deps or leak build-machine paths (issue #1 guard)
        run: |
          if grep -rq "/Users/" dist/; then
            echo "::error::build-machine absolute path leaked into dist"; exit 1
          fi
          # ALL five optional deps must stay as runtime specifiers in BOTH
          # bundle formats — a missing one means it was inlined or dropped
          for bundle in dist/index.js dist/index.cjs; do
            for dep in jieba-wasm @sglkc/kuromoji wordcut snowball-stemmers tinyld; do
              if ! grep -q "\"$dep\"" "$bundle"; then
                echo "::error::$dep specifier missing from $bundle — inlined or dropped?"; exit 1
              fi
            done
            # an inlined bundle is ~1MB; external build is ~20KB
            size=$(wc -c < "$bundle")
            if [ "$size" -gt 200000 ]; then
              echo "::error::$bundle is ${size} bytes — optional deps likely inlined again"; exit 1
            fi
          done

  node-compat:
    name: Node ${{ matrix.node }} CJS/ESM smoke
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node: [18, 20, 22]
    steps:
      - uses: actions/checkout@v4
      - uses: oven-sh/setup-bun@v2
      - uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node }}
      - run: bun install
      - run: bun run build
      - name: CJS require smoke (the Node <22 breakage guard)
        run: |
          node -e "
            const m = require('./dist/index.cjs');
            if (m.detectLanguage('机器学习') !== 'zh') process.exit(1);
            if (!m.tokenizeForFts('机器学习', 'zh')) process.exit(1);
            console.log('CJS require OK on', process.version);
          "
      - name: ESM import smoke (no MODULE_TYPELESS warning tolerated)
        run: |
          out=$(node --no-warnings=ExperimentalWarning -e "
            import('./dist/index.js').then(m => {
              if (m.detectLanguage('東京タワーです') !== 'ja') process.exit(1);
              console.log('ESM import OK on', process.version);
            });
          " 2>&1)
          echo "$out"
          if echo "$out" | grep -q "MODULE_TYPELESS_PACKAGE_JSON"; then
            echo "double-parse warning detected"; exit 1
          fi

  no-optional-deps:
    name: zero-dependency fallback chain
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: oven-sh/setup-bun@v2
      - run: bun install
      - run: bun run build
      - name: run smoke in a dir with no node_modules
        run: |
          mkdir -p /tmp/bm-smoke
          cp -r dist scripts/smoke-no-optional.mjs /tmp/bm-smoke/
          # package.json must come along: it carries "type":"module" so the
          # ESM bundle resolves the way a real npm install would, instead of
          # relying on Node's ambiguous-.js reparse fallback
          cp package.json /tmp/bm-smoke/
          cd /tmp/bm-smoke
          node smoke-no-optional.mjs
